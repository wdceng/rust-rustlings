use std::arch::aarch64::int16x4x3_t;

fn main() {
    // TODO: Change the line below to fix the compiler error.
    let x: i32 = 10;

    if x == 10 {
        println!("x is ten!");
    } else {
        println!("x is not ten!");
    }
}
